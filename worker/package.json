{
  "name": "neurofamilia-rag-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "ingest": "wrangler dev --remote src/scripts/ingest.ts",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260826.1",
    "typescript": "^5.7.2",
    "wrangler": "^4.127.0"
  }
}
