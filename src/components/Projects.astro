---
const projects = [
  {
    title: "Cybersentinel",
    description: "Ecosistema fintech en Cloudflare Workers: wallet cripto/COP, exchange, trading con IA. Integraciones reales con Bancolombia, Wompi y Truora, auth JWT compartida entre 7 servicios.",
    tags: ["Cloudflare Workers", "TypeScript", "D1"],
    year: "2026",
    link: "https://wallet.dsltdev.com",
    github: null
  },
  {
    title: "DSLT Señales",
    description: "SaaS de señales de trading para LATAM: backtesting histórico, sizing con criterio de Kelly, ejecución automática opcional, cobros con Wompi.",
    tags: ["Python", "FastAPI", "RL"],
    year: "2026",
    link: "https://saas.dsltdev.com",
    github: null
  },
  {
    title: "Barbería SaaS",
    description: "Marketplace de agendamiento para barberías: descubrimiento por ciudad, reservas en tiempo real, sincronización en vivo vía Supabase Realtime.",
    tags: ["Next.js", "React 19", "Supabase"],
    year: "2026",
    link: null,
    github: "https://github.com/dsltdev/barberia-saas"
  },
  {
    title: "JARVIS",
    description: "Asistente personal de terminal: agente con tools de shell/git/filesystem, memoria persistente en SQLite, soporta modelos locales (Ollama) o Claude.",
    tags: ["TypeScript", "SQLite", "Ollama"],
    year: "2026",
    link: null,
    github: null
  }
];
---

<section id="projects" class="projects">
  <div class="container">
    <div class="projects-header">
      <div>
        <p class="section-label">// 002</p>
        <h2 class="section-title">Proyectos</h2>
      </div>
      <a href="https://github.com/dsltdev" class="btn btn-secondary" target="_blank">GitHub →</a>
    </div>

    <div class="projects-grid">
      {projects.map((project, index) => (
        <article class="project-card">
          <div class="project-number">{String(index + 1).padStart(2, '0')}</div>
          
          <div class="project-content">
            <div class="project-header">
              <h3 class="project-title">{project.title}</h3>
              <span class="project-year">{project.year}</span>
            </div>
            
            <p class="project-description">{project.description}</p>
            
            <div class="project-tags">
              {project.tags.map(tag => (
                <span class="tag">{tag}</span>
              ))}
            </div>
          </div>

          {(project.link || project.github) && (
            <div class="project-links">
              {project.link && <a href={project.link} class="project-link" target="_blank" rel="noopener">Ver ↗</a>}
              {project.github && <a href={project.github} class="project-link" target="_blank" rel="noopener">Código ↗</a>}
            </div>
          )}
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .projects-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 3rem;
  }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.25rem;
    background: transparent;
  }

  .project-card {
    background: var(--glass-bg);
    backdrop-filter: blur(var(--glass-blur));
    -webkit-backdrop-filter: blur(var(--glass-blur));
    border: 2px solid var(--glass-border);
    border-radius: var(--radius-lg);
    box-shadow: var(--glass-shadow);
    padding: 2.5rem;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    transition: all 0.4s var(--ease-out);
    position: relative;
    overflow: hidden;
  }

  .project-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, var(--accent), var(--secondary), var(--tertiary));
    opacity: 0;
    transition: opacity 0.4s ease;
  }

  .project-card:hover {
    background: var(--glass-bg-hover);
    border-color: var(--accent);
    box-shadow: 0 20px 60px rgba(99, 102, 241, 0.2), 0 8px 24px rgba(236, 72, 153, 0.1);
    transform: translateY(-6px);
  }

  .project-card:hover::before {
    opacity: 1;
  }

  .project-number {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--accent);
    opacity: 0.7;
    padding: 0.3rem 0.7rem;
    background: rgba(99, 102, 241, 0.1);
    border-radius: 50px;
    display: inline-block;
  }

  .project-content {
    flex: 1;
  }

  .project-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 1rem;
  }

  .project-title {
    font-family: var(--font-display);
    font-size: 1.4rem;
    font-weight: 700;
    letter-spacing: -0.01em;
    color: var(--text);
  }

  .project-year {
    font-family: var(--font-sans);
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--accent);
    padding: 0.3rem 0.7rem;
    background: rgba(99, 102, 241, 0.1);
    border-radius: 50px;
  }

  .project-description {
    color: var(--text-light);
    font-size: 1rem;
    line-height: 1.7;
    margin-bottom: 1.2rem;
  }

  .project-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
  }

  .tag {
    font-family: var(--font-sans);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    padding: 0.4rem 0.9rem;
    background: linear-gradient(135deg, rgba(99, 102, 241, 0.08), rgba(236, 72, 153, 0.08));
    color: var(--accent);
    border: 1px solid var(--glass-border);
    border-radius: 50px;
    transition: all 0.3s ease;
  }

  .tag:hover {
    background: linear-gradient(135deg, rgba(99, 102, 241, 0.15), rgba(236, 72, 153, 0.15));
    border-color: var(--accent);
    transform: translateY(-1px);
  }

  .project-links {
    display: flex;
    gap: 1rem;
    padding-top: 1.2rem;
    border-top: 2px solid var(--border);
  }

  .project-link {
    font-family: var(--font-sans);
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.03em;
    color: var(--accent);
    transition: all 0.3s var(--ease-out);
    position: relative;
    padding: 0.4rem 0;
  }

  .project-link::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background: linear-gradient(90deg, var(--accent), var(--secondary));
    transition: width 0.3s var(--ease-out);
  }

  .project-link:hover {
    color: var(--accent-dim);
    transform: translateX(2px);
  }

  .project-link:hover::after {
    width: 100%;
  }

  @media (max-width: 768px) {
    .projects-grid {
      grid-template-columns: 1fr;
    }

    .projects-header {
      flex-direction: column;
      gap: 1rem;
    }
  }

  @media (prefers-reduced-transparency: reduce) {
    .project-card {
      background: rgba(12, 22, 16, 0.98);
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
    }
  }
</style>
